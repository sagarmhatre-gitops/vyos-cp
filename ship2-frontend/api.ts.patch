// =====================================================================
// Ship 2 — additions for /opt/vyos-cp/frontend/src/lib/api.ts
//
// Two changes:
//   A) Insert the type block BEFORE the line `export const api = new API();`
//   B) Insert the two method declarations INSIDE the API class, right after
//      the `captureSnapshotNow(...)` method that Ship 1 added.
//
// Both changes are pure additions — no existing code is removed or edited.
// =====================================================================


// ---- (A) TYPES — paste just before `export const api = new API();` -------

export type DiffOp = 'add' | 'remove' | 'modify';

export type DiffChange = {
  path: string;
  op: DiffOp;
  before?: unknown;
  after?: unknown;
};

export type SnapshotDiff = {
  from: number;
  to: number;
  changes: DiffChange[];
};


// ---- (B) METHODS — paste inside the API class, after captureSnapshotNow --

  // Ship 2 — fetch a specific historical snapshot (full config).
  getSnapshotByID(deviceID: string, snapshotID: number) {
    return this.req<DeviceSnapshot>(
      `/api/v1/devices/${deviceID}/snapshots/${snapshotID}`,
    );
  }

  // Ship 2 — compute the diff between two snapshots. `to` defaults to 'latest'.
  computeDiff(
    deviceID: string,
    fromID: number,
    toID: number | 'latest' = 'latest',
  ) {
    const t = typeof toID === 'number' ? String(toID) : toID;
    return this.req<SnapshotDiff>(
      `/api/v1/devices/${deviceID}/diff?from=${fromID}&to=${t}`,
    );
  }
